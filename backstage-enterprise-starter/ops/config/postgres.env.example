# Local PostgreSQL on the same VM:
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_DB=backstage
POSTGRES_USER=backstage
POSTGRES_PASSWORD=CHANGE_ME

# For external PostgreSQL, replace POSTGRES_HOST with the real host.
